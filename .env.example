# Hermes voice agent webhook. The /api/lead route validates and forwards
# leads here. Until this is set the form validates and returns success
# without dispatching, so it stays testable.
HERMES_WEBHOOK_URL=
